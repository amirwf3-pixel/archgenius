{
  "name": "archgenius",
  "private": true,
  "version": "1.1.0",
  "description": "ArchGenius — AI Architectural Planning & Professional AutoCAD System",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:core": "npm run test -w @archgenius/core",
    "dev": "npm run dev -w @archgenius/web",
    "typecheck": "npm run typecheck -w @archgenius/core && npm run typecheck -w @archgenius/web"
  },
  "engines": {
    "node": ">=20"
  }
}
