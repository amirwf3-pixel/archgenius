/**
 * Stair placement constants.
 *
 * Phase 4 replaces the V1 rectangular stair placeholder with a full
 * geometry engine (see stair-solver.ts). This file keeps the shared
 * floor-height constant used across the generator.
 */

/** Floor-to-floor default height (m). */
export const DEFAULT_FLOOR_HEIGHT = 3.20;
